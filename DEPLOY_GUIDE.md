# 🚀 Инструкция по деплою для GitHub Pages

Ваше приложение: https://dahaky.github.io/wim-hof-breathing/

## Шаг 1: Деплой бэкенда на Vercel (БЕСПЛАТНО)

### 1.1 Подготовка
1. Зарегистрируйтесь на [vercel.com](https://vercel.com) через GitHub
2. Установите Vercel CLI (опционально):
```bash
npm i -g vercel
```

### 1.2 Деплой через веб-интерфейс (РЕКОМЕНДУЕТСЯ)

1. Откройте [vercel.com/new](https://vercel.com/new)
2. Выберите ваш репозиторий `dahaky/wim-hof-breathing`
3. В настройках проекта:
   - **Root Directory**: `server`
   - **Build Command**: оставьте пустым
   - **Output Directory**: оставьте пустым
   - **Install Command**: `npm install`

4. Добавьте переменную окружения (опционально):
   - Key: `BOT_TOKEN`
   - Value: `ваш_telegram_bot_token` (получите у @BotFather)

5. Нажмите **Deploy**

6. После деплоя скопируйте URL (например: `https://your-project.vercel.app`)

### 1.3 Деплой через CLI (альтернатива)

```bash
cd server
vercel

# Следуйте инструкциям:
# - Set up and deploy? Yes
# - Which scope? Your account
# - Link to existing project? No
# - Project name? wim-hof-stats
# - Directory? ./
# - Override settings? No

# После деплоя скопируйте Production URL
```

## Шаг 2: Обновите URL в приложении

Откройте файл [`app.js`](app.js) и измените строку 4:

```javascript
// Замените на ваш Vercel URL
const API_URL = 'https://your-project.vercel.app/api';
```

Например:
```javascript
const API_URL = 'https://wim-hof-stats.vercel.app/api';
```

## Шаг 3: Закоммитьте изменения

```bash
git add app.js
git commit -m "Update API URL for production"
git push origin main
```

GitHub Pages автоматически обновится через 1-2 минуты.

## Шаг 4: Проверка работы

1. Откройте https://dahaky.github.io/wim-hof-breathing/
2. На главном экране должна появиться статистика
3. Завершите практику - данные должны отправиться на сервер
4. Проверьте статистику: `https://your-project.vercel.app/api/stats/all`

## 🔒 Безопасность

### Текущая защита:
✅ CORS настроен только для вашего домена  
✅ Валидация Telegram initData (если указан BOT_TOKEN)  
✅ Защита от дубликатов (1 запись в день на пользователя)  
✅ Автоматическая очистка старых данных (>7 дней)  

### Для публичного проекта:
- Код сервера виден всем, но это нормально
- API URL виден в app.js, но это тоже нормально
- Данные хранятся в памяти сервера (при перезапуске сбрасываются)
- Для постоянного хранения используйте базу данных (см. ниже)

## 📊 Мониторинг

### Просмотр статистики:
```
https://your-project.vercel.app/api/stats/all
```

Ответ:
```json
{
  "totalCompletions": 45,
  "uniqueUsers": 23,
  "todayCount": 12,
  "lastUpdate": "2026-02-06T08:52:00.000Z"
}
```

### Логи Vercel:
1. Откройте [vercel.com/dashboard](https://vercel.com/dashboard)
2. Выберите проект
3. Вкладка **Logs** - здесь видны все запросы

## 🔄 Обновление сервера

После изменений в `server/`:

```bash
cd server
vercel --prod
```

Или просто сделайте `git push` - Vercel автоматически задеплоит изменения.

## 💾 Постоянное хранилище (опционально)

Сейчас данные хранятся в памяти и сбрасываются при перезапуске. Для постоянного хранения:

### Вариант A: Vercel KV (Redis) - БЕСПЛАТНО
```bash
# В Vercel Dashboard
# Storage → Create Database → KV
# Подключите к проекту
```

Обновите `server.js`:
```javascript
const { kv } = require('@vercel/kv');

// Сохранение
await kv.set('stats', statsData);

// Получение
const statsData = await kv.get('stats') || { completions: [] };
```

### Вариант B: MongoDB Atlas - БЕСПЛАТНО
```bash
npm install mongodb
```

Зарегистрируйтесь на [mongodb.com/cloud/atlas](https://www.mongodb.com/cloud/atlas)

### Вариант C: Supabase - БЕСПЛАТНО
```bash
npm install @supabase/supabase-js
```

Зарегистрируйтесь на [supabase.com](https://supabase.com)

## 🐛 Troubleshooting

### Статистика не загружается
1. Проверьте URL в `app.js` (строка 4)
2. Откройте консоль браузера (F12) → вкладка Console
3. Проверьте работу API: `https://your-project.vercel.app/health`

### CORS ошибки
Убедитесь, что в `server.js` указан правильный origin:
```javascript
origin: ['https://dahaky.github.io', ...]
```

### Vercel деплой не работает
1. Проверьте `vercel.json` в папке `server/`
2. Убедитесь, что Root Directory = `server`
3. Проверьте логи в Vercel Dashboard

### Данные сбрасываются
Это нормально для in-memory хранилища. Используйте базу данных (см. выше).

## 📱 Telegram Bot Token (опционально)

Для валидации данных Telegram:

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Скопируйте токен
3. В Vercel Dashboard → Settings → Environment Variables:
   - Key: `BOT_TOKEN`
   - Value: `ваш_токен`
4. Redeploy проект

## 🎯 Альтернативные бесплатные хостинги

### Railway.app
```bash
# Установите Railway CLI
npm i -g @railway/cli

# Деплой
cd server
railway login
railway init
railway up
```

### Render.com
1. Зарегистрируйтесь на [render.com](https://render.com)
2. New → Web Service
3. Connect GitHub repo
4. Root Directory: `server`
5. Build Command: `npm install`
6. Start Command: `node server.js`

### Fly.io
```bash
# Установите flyctl
curl -L https://fly.io/install.sh | sh

# Деплой
cd server
fly launch
fly deploy
```

## 📞 Поддержка

Если что-то не работает:
1. Проверьте консоль браузера (F12)
2. Проверьте логи Vercel
3. Проверьте `/health` endpoint
4. Откройте issue в GitHub репозитории

## ✅ Чеклист

- [ ] Зарегистрировались на Vercel
- [ ] Задеплоили сервер
- [ ] Скопировали Production URL
- [ ] Обновили `API_URL` в `app.js`
- [ ] Закоммитили и запушили изменения
- [ ] Проверили работу на https://dahaky.github.io/wim-hof-breathing/
- [ ] Проверили статистику через `/api/stats/all`

Готово! 🎉
